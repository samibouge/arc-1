{
  "$schema": "https://unpkg.com/@anthropic-ai/mcpb@latest/schemas/mcpb-manifest-v0.3.schema.json",
  "manifest_version": "0.3",
  "name": "arc-1",
  "display_name": "ARC-1 - SAP ABAP MCP Server",
  "version": "0.10.0",
  "description": "MCP server for SAP ABAP Development Tools (ADT). Read, write, search, activate, and manage ABAP objects in SAP systems.",
  "long_description": "ARC-1 connects Claude to SAP ABAP systems via the ABAP Development Tools (ADT) REST API. Provides 12 intent-based tools covering the full ABAP development lifecycle: read source code, create/update objects, search, activate, navigate references, run SQL queries, manage transports and Git, lint, and diagnose runtime issues. Read-only by default with configurable safety gates. Supports on-premise SAP and BTP ABAP Environment.",
  "icon": "icon.png",
  "author": {
    "name": "Marian Zeis",
    "url": "https://github.com/marianfoo"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/arc-mcp/arc-1.git"
  },
  "homepage": "https://docs.arc-1-mcp.com/",
  "documentation": "https://docs.arc-1-mcp.com/quickstart/",
  "support": "https://github.com/arc-mcp/arc-1/issues",
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/dist/index.js"
      ],
      "env": {
        "SAP_URL": "${user_config.sap_url}",
        "SAP_USER": "${user_config.sap_user}",
        "SAP_PASSWORD": "${user_config.sap_password}",
        "SAP_CLIENT": "${user_config.sap_client}",
        "SAP_LANGUAGE": "${user_config.sap_language}",
        "SAP_INSECURE": "${user_config.sap_insecure}",
        "SAP_ALLOW_WRITES": "${user_config.sap_allow_writes}",
        "SAP_ALLOWED_PACKAGES": "${user_config.sap_allowed_packages}",
        "SAP_ALLOW_DATA_PREVIEW": "${user_config.sap_allow_data_preview}",
        "SAP_ALLOW_FREE_SQL": "${user_config.sap_allow_free_sql}",
        "SAP_ALLOW_TRANSPORT_WRITES": "${user_config.sap_allow_transport_writes}",
        "SAP_ALLOW_GIT_WRITES": "${user_config.sap_allow_git_writes}",
        "ARC1_UI": "${user_config.arc1_ui}",
        "ARC1_UI_OPEN": "${user_config.arc1_ui_open}",
        "ARC1_UI_ADDR": "${user_config.arc1_ui_addr}"
      }
    }
  },
  "tools": [
    {
      "name": "SAPRead",
      "description": "Read SAP ABAP objects (programs, classes, interfaces, functions, CDS, DDIC, etc.)"
    },
    {
      "name": "SAPWrite",
      "description": "Create or update ABAP source code and DDIC metadata"
    },
    {
      "name": "SAPSearch",
      "description": "Search for ABAP objects by name, pattern, or content"
    },
    {
      "name": "SAPActivate",
      "description": "Activate ABAP objects, run syntax checks, unit tests, and ATC checks"
    },
    {
      "name": "SAPNavigate",
      "description": "Navigate code: find definitions, references, where-used, completions"
    },
    {
      "name": "SAPQuery",
      "description": "Preview table data or run SQL queries"
    },
    {
      "name": "SAPTransport",
      "description": "Manage CTS transport requests"
    },
    {
      "name": "SAPGit",
      "description": "Version control for ABAP via gCTS or abapGit (status, clone, pull, commit, push) — available when a Git backend is present"
    },
    {
      "name": "SAPContext",
      "description": "Get compressed dependency context for ABAP objects (7-30x token savings)"
    },
    {
      "name": "SAPLint",
      "description": "Lint ABAP source code with abaplint rules"
    },
    {
      "name": "SAPDiagnose",
      "description": "Analyze short dumps, profiler traces, and runtime diagnostics"
    },
    {
      "name": "SAPManage",
      "description": "Delete ABAP objects or manage object lifecycle"
    }
  ],
  "keywords": [
    "sap",
    "abap",
    "adt",
    "erp",
    "s4hana",
    "btp",
    "development"
  ],
  "license": "MIT",
  "compatibility": {
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=22.22.1"
    }
  },
  "user_config": {
    "sap_url": {
      "type": "string",
      "title": "SAP System URL",
      "description": "URL of your SAP system (e.g. http://host:50000 or https://host:44300).",
      "required": true
    },
    "sap_user": {
      "type": "string",
      "title": "SAP Username",
      "description": "Your SAP user ID for authentication.",
      "required": true
    },
    "sap_password": {
      "type": "string",
      "title": "SAP Password",
      "description": "Your SAP password (stored securely in the OS keychain).",
      "sensitive": true,
      "required": true
    },
    "sap_client": {
      "type": "string",
      "title": "SAP Client",
      "description": "SAP logon client number.",
      "default": "100",
      "required": false
    },
    "sap_language": {
      "type": "string",
      "title": "SAP Language",
      "description": "SAP logon language and master language for created objects.",
      "default": "EN",
      "required": false
    },
    "sap_insecure": {
      "type": "boolean",
      "title": "Skip TLS Verification",
      "description": "Skip TLS certificate verification (only for self-signed dev/test systems).",
      "default": false,
      "required": false
    },
    "sap_allow_writes": {
      "type": "boolean",
      "title": "Allow Writes",
      "description": "Allow ABAP object mutations (create/update/activate). Prerequisite for transport and Git writes. Keep off for read/search-only usage.",
      "default": false,
      "required": false
    },
    "sap_allowed_packages": {
      "type": "string",
      "title": "Allowed Packages (write scope)",
      "description": "Comma-separated write allowlist: exact names, Z* wildcards, ZFOO/** subtrees, or * for ALL packages. Default $TMP (local/throwaway only); leave empty to keep the $TMP default. Only applies when Allow Writes is on.",
      "default": "$TMP",
      "required": false
    },
    "sap_allow_data_preview": {
      "type": "boolean",
      "title": "Allow Table Data Preview",
      "description": "Allow SAPRead(type=TABLE_CONTENTS) and SAPQuery table preview. Can expose application data — review your data-governance rules.",
      "default": false,
      "required": false
    },
    "sap_allow_free_sql": {
      "type": "boolean",
      "title": "Allow Freestyle SQL",
      "description": "Allow read-only ad-hoc ABAP SQL via SAPQuery. Can expose application data — review your data-governance rules.",
      "default": false,
      "required": false
    },
    "sap_allow_transport_writes": {
      "type": "boolean",
      "title": "Allow Transport Writes",
      "description": "Allow CTS transport mutations (create/assign/release). Also requires Allow Writes.",
      "default": false,
      "required": false
    },
    "sap_allow_git_writes": {
      "type": "boolean",
      "title": "Allow Git Writes",
      "description": "Allow gCTS / abapGit mutations (commit/push/pull). Also requires Allow Writes.",
      "default": false,
      "required": false
    },
    "arc1_ui": {
      "type": "boolean",
      "title": "Enable Read-only UI",
      "description": "Experimental and off by default. Start a loopback browser console for sanitized config, cache metadata, feature status, and recent sanitized logs.",
      "default": false,
      "required": false
    },
    "arc1_ui_open": {
      "type": "boolean",
      "title": "Open UI on Startup",
      "description": "Open the read-only UI in your browser when ARC-1 starts. Only applies when the UI is enabled.",
      "default": false,
      "required": false
    },
    "arc1_ui_addr": {
      "type": "string",
      "title": "UI Loopback Address",
      "description": "Loopback bind address for the local read-only UI.",
      "default": "127.0.0.1:8711",
      "required": false
    }
  }
}
