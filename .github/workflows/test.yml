name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Only one SAP-hitting workflow at a time — prevents parallel runs from
# different PRs/pushes from exhausting SAP work processes.
concurrency:
  group: sap-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22, 24]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run typecheck

      - name: Run unit tests
        run: npm test

      - name: Upload unit test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results-unit-${{ matrix.node-version }}
          path: test-results/unit.json
          retention-days: 7
          if-no-files-found: warn

      # Coverage — informational only, does not block the build
      - name: Run coverage (informational)
        run: npm run test:coverage
        continue-on-error: true

      - name: Coverage summary
        if: always()
        run: node scripts/ci/coverage-summary.mjs
        continue-on-error: true

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-${{ matrix.node-version }}
          path: coverage/
          retention-days: 7
        continue-on-error: true

  # Run on push (main) and internal PRs; skip external fork PRs (no secrets)
  integration:
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'push' ||
      (github.event_name == 'pull_request' &&
       github.event.pull_request.head.repo.full_name == github.repository)
    needs: test

    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: SAP auth preflight (integration)
        id: sap_auth
        shell: bash
        env:
          TEST_SAP_URL: ${{ secrets.TEST_SAP_URL }}
          TEST_SAP_USER: ${{ secrets.TEST_SAP_USER }}
          TEST_SAP_PASSWORD: ${{ secrets.TEST_SAP_PASSWORD }}
          TEST_SAP_CLIENT: ${{ secrets.TEST_SAP_CLIENT }}
        run: |
          base_url="${TEST_SAP_URL%/}"
          probe_url="${base_url}/sap/bc/adt/core/discovery?sap-client=${TEST_SAP_CLIENT}"
          status="$(curl --silent --show-error --location --output /tmp/adt-discovery-integration.xml --write-out "%{http_code}" \
            -u "${TEST_SAP_USER}:${TEST_SAP_PASSWORD}" "${probe_url}" || true)"
          echo "status=${status}"
          if [ "${status}" = "401" ]; then
            echo "auth_ok=false" >> "$GITHUB_OUTPUT"
            echo "::warning title=Integration skipped::SAP auth preflight returned HTTP 401 (logon failed). Skipping integration tests for this run."
          else
            echo "auth_ok=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Run integration tests
        if: steps.sap_auth.outputs.auth_ok == 'true'
        run: npm run test:integration
        env:
          TEST_SAP_URL: ${{ secrets.TEST_SAP_URL }}
          TEST_SAP_USER: ${{ secrets.TEST_SAP_USER }}
          TEST_SAP_PASSWORD: ${{ secrets.TEST_SAP_PASSWORD }}
          TEST_SAP_CLIENT: ${{ secrets.TEST_SAP_CLIENT }}
          TEST_SAP_INSECURE: ${{ secrets.TEST_SAP_INSECURE }}

      - name: Upload integration test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results-integration
          path: test-results/integration.json
          retention-days: 7
          if-no-files-found: warn

      - name: Integration reliability summary
        if: always()
        run: node scripts/ci/collect-test-reliability.mjs --results-dir test-results
        continue-on-error: true

  # Run on push (main) and internal PRs; skip external fork PRs (no secrets)
  # Runs AFTER integration to avoid both jobs hitting SAP simultaneously.
  # Uses always() so e2e still runs even if integration fails (SAP flakes).
  e2e:
    runs-on: ubuntu-latest
    if: >
      always() &&
      needs.test.result == 'success' &&
      (github.event_name == 'push' ||
       (github.event_name == 'pull_request' &&
        github.event.pull_request.head.repo.full_name == github.repository))
    needs: [test, integration]
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: SAP auth preflight (e2e)
        id: sap_auth
        shell: bash
        env:
          SAP_URL: ${{ secrets.SAP_URL }}
          SAP_USER: ${{ secrets.SAP_USER }}
          SAP_PASSWORD: ${{ secrets.SAP_PASSWORD }}
          SAP_CLIENT: '001'
        run: |
          base_url="${SAP_URL%/}"
          probe_url="${base_url}/sap/bc/adt/core/discovery?sap-client=${SAP_CLIENT}"
          status="$(curl --silent --show-error --location --output /tmp/adt-discovery-e2e.xml --write-out "%{http_code}" \
            -u "${SAP_USER}:${SAP_PASSWORD}" "${probe_url}" || true)"
          echo "status=${status}"
          if [ "${status}" = "401" ]; then
            echo "auth_ok=false" >> "$GITHUB_OUTPUT"
            echo "::warning title=E2E skipped::SAP auth preflight returned HTTP 401 (logon failed). Skipping E2E tests for this run."
          else
            echo "auth_ok=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Build
        if: steps.sap_auth.outputs.auth_ok == 'true'
        run: npm run build

      - name: Start MCP server (local)
        if: steps.sap_auth.outputs.auth_ok == 'true'
        run: npm run test:e2e:start
        env:
          SAP_URL: ${{ secrets.SAP_URL }}
          SAP_USER: ${{ secrets.SAP_USER }}
          SAP_PASSWORD: ${{ secrets.SAP_PASSWORD }}
          SAP_CLIENT: '001'

      - name: Run E2E tests
        if: steps.sap_auth.outputs.auth_ok == 'true'
        run: npm run test:e2e
        env:
          E2E_MCP_URL: http://localhost:3000/mcp
          SAP_URL: ${{ secrets.SAP_URL }}
          SAP_USER: ${{ secrets.SAP_USER }}
          SAP_PASSWORD: ${{ secrets.SAP_PASSWORD }}
          SAP_CLIENT: '001'

      - name: Upload E2E JSON test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results-e2e
          path: test-results/e2e.json
          retention-days: 7
          if-no-files-found: warn

      - name: E2E reliability summary
        if: always()
        run: node scripts/ci/collect-test-reliability.mjs --results-dir test-results
        continue-on-error: true

      - name: Stop MCP server
        if: always()
        run: npm run test:e2e:stop

      - name: Upload E2E logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: e2e-logs
          path: /tmp/arc1-e2e-logs/
          retention-days: 7

      - name: Upload JUnit results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: e2e-junit
          path: /tmp/arc1-e2e-logs/junit-results.xml
          retention-days: 7

  reliability-summary:
    if: always()
    runs-on: ubuntu-latest
    needs: [test, integration, e2e]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Download test result artifacts
        if: always()
        uses: actions/download-artifact@v5
        with:
          pattern: test-results-*
          path: downloaded-test-results
          merge-multiple: true

      - name: Normalize test result files
        if: always()
        run: |
          mkdir -p test-results
          find downloaded-test-results -name unit.json -print -quit | xargs -I{} cp {} test-results/unit.json || true
          find downloaded-test-results -name integration.json -print -quit | xargs -I{} cp {} test-results/integration.json || true
          find downloaded-test-results -name e2e.json -print -quit | xargs -I{} cp {} test-results/e2e.json || true

      - name: Collect reliability summary
        if: always()
        run: npm run test:reliability-report -- --results-dir test-results
        continue-on-error: true

      - name: Assert required execution (warn mode)
        if: always()
        run: npm run test:assert-execution -- --results-dir test-results --mode warn
        continue-on-error: true
